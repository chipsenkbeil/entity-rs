[package]
name = "inmemory-example"
version = "0.0.0"
publish = false
edition = "2018"

[dependencies]
entity = { version = "0.2.0", path = "../../entity", features = ["global", "inmemory_db", "macros"] }
